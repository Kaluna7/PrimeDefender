/** Incidents kept in Monitoring state (socket + DB merge). */
export const MAX_LIVE_ATTACKS = 1001;
/** Counts above this value use the unlimited symbol in protection cards. */
export const PROTECTION_COUNT_UNLIMITED_THRESHOLD = 1000;
/** Keep map rendering bounded independently from counters and feed data. */
export const MAX_MAP_ARCS = 100;
